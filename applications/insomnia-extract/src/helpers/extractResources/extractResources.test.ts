describe('extractResources', () => {
  it.todo('should return an empty array if input is empty');
  it.todo('should throw an error if root resource cannot be found');
  it.todo('should only return the root resource if no child resources are found');
  it.todo('should return the root resource along with its child resources(e.g. subfolders)');
});
